#!/usr/bin/env perl
use strict;
use warnings;
use Bot::Games;

Bot::Games->new(
    server   => 'irc.efnet.net',
    channels => ['#netmonster'],
    nick     => 'doygames',
    username => 'doygames',
    name     => 'doygames',
    prefix   => '@',
)->run;
